.venv/
__pycache__/
.mypy_cache/
.ruff_cache/
submission.zip
*.pgn

# Deep Blue local artefacts
submission.zip
*.pgn
logs/

# Large/derived NNUE artefacts -- not needed for CI, keep pushes lean
.numba_cache/
nnue_lab/teammate_pawnstar/finetune/checkpoints/
nnue_lab/production/.numba_cache/
nnue_lab/production/.mypy_extension_cache/

# Session scratch: sampled Stockfish data and derived arrays. Regenerate with
# tools/sample_sf_positions.py, tools/filter_quiet.py, tools/extract_terms.py.
scratch_*.txt
scratch_*.npz
scratch_*.npy
scratch_*.pkl

# NNUE training data: distributed as GitHub RELEASE ASSETS (up to 2GB each),
# never committed -- git rejects files over 100MB and keeps every blob forever.
# See the nnue-data-v1 release and NNUE_TRAINING_HANDOFF.md.
nnue_train_*.npz
nnue_validation_*.npz
scratch_*.log
